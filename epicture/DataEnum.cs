using System;
using System.Collections.Generic;
using System.Text;

namespace epicture
{
    public enum DataEnum
    {
        Authentification = 0,
        AccountImages = 1,
        GallerySearch = 2,
        Upload = 3,
        Favorites = 4,
        DeleteImage = 5,
        FavoriteImage = 6
    }
}
